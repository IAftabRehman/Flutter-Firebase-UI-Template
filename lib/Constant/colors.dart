import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (buttons, highlights, etc.)
  static const Color primary = Color(0xFF339D44); // Green

  // Secondary color (optional)
  static const Color secondary = Color(0xFF5DB075); // Light green

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFFFFFF); // white
  static const Color backgroundGray = Color(0xFFF5F5F5);  // light gray

  // Text colors
  static const Color textPrimary = Color(0xFF292929); // Dark gray
  static const Color textSecondary = Color(0xFF808080); // lighter gray
  static const Color textWhite = Color(0xFFFFFFFF);

  // Borders & Lines
  static const Color border = Color(0xFFE0E0E0);

  // Icons
  static const Color iconInactive = Color(0xFFD4D4D4);

  // Error / Alert
  static const Color error = Color(0xFFE74C3C); // red
  static const Color success = Color(0xFF27AE60); // green check
}
